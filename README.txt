INSTRUCTIONS:

To run the code, use the commandline: ruby sentence_reversal.rb

This will make a file called solution.txt where the solution is stored. It will also print out the solution.

NOTE: Make sure the programming_test.txt file is in the same folder as the sentence_reversal.rb file.